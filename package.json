{
  "name": "node_http",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch '**/*.ts' --exec node --loader ts-node/esm src/server.ts",
    "prod": "cross-env NODE_ENV=production nodemon --watch '**/*.ts' --exec node --loader ts-node/esm src/server.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "dayjs": "^1.11.19",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "cross-env": "^10.1.0",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
